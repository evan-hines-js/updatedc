[package]
name = "updatec-e2e"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
chrono.workspace = true
# The lifecycle reconciler the green release runs owns the provider timeout it is published with,
# so the value signed into the provider set here and the reconciler's own apply budget are one
# constant.
demo-lifecycle = { path = "../demo-lifecycle" }
k8s-openapi.workspace = true
kube.workspace = true
reqwest.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
updated = { path = "../updated" }
updated-contracts = { path = "../updated-contracts" }
updatec = { path = "../updatec" }

[dev-dependencies]
tempfile.workspace = true

[features]
fips = ["updatec/fips", "updated/fips"]
