{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://updated.dev/schemas/target-reference.schema.json",
  "title": "Exact TUF Target Reference",
  "description": "An immutable reference to one target authenticated by TUF metadata.",
  "type": "object",
  "additionalProperties": false,
  "required": ["path", "sha256"],
  "properties": {
    "path": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "pattern": "^(?!.*(?:^|/)\\.{1,2}(?:/|$))[^/\\\\:\\u0000-\\u001f\\u007f-\\u009f]+(?:/[^/\\\\:\\u0000-\\u001f\\u007f-\\u009f]+)*$",
      "description": "A confined relative target name: one or more non-empty `/`-separated components, none of them `.` or `..`, and none containing a backslash, a drive/scheme colon, or a control character. Joined onto any root it can never escape it."
    },
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    }
  }
}
