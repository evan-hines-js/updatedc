target/
/e2e/.demo
*.old
# Signing material must never be committed. Only the *public* key is embedded in
# the app (at build time), and even that is passed via env, not stored here.
keys/
*.key

# Coverage instrumentation artifacts.
*.profraw
*.profdata
mutants.out*/
# Added by code-review-graph
.code-review-graph/

# The Proxmox chaos lab keeps credentials, Terraform state, generated
# inventories, kubeconfigs, plans, and downloaded artifacts outside Git.
/lab/chaos/deploy.env
/lab/chaos/infrastructure/.state/
/lab/chaos/infrastructure/terraform/.terraform/
/lab/chaos/infrastructure/terraform/*.tfstate*
/lab/chaos/infrastructure/terraform/*.tfvars
/lab/chaos/infrastructure/terraform/.terraform.tfstate.lock.info
