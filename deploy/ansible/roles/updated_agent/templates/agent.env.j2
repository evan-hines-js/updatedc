# Managed by the updated_agent Ansible role. Read by the updated-agent unit, which carries these
# same values as its own defaults — so this file only ever overrides paths.
UPDATED_STATE_DIR=/var/lib/updated
UPDATED_CONFIG=/etc/updated/config.toml
