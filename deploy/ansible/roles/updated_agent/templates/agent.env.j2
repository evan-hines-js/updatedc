# Managed by the updated_agent Ansible role. Read by the updated-agent unit, which carries these
# same values as its own defaults — so this file only ever overrides.
UPDATED_READY_TIMEOUT={{ updated_ready_timeout }}
UPDATED_CONFIRM_TIMEOUT={{ updated_confirm_timeout }}
